#disgonbe a onelinerfuckingmess

import string
import random


print ''.join([random.choice(string.letters + string.digits + string.punctuation) for a in range(0, int(input("How long do you want the password to be?(I recomend at least 10 characters) ")))])

